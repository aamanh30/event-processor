import { CaptureEventDirective } from './capture-event.directive';

describe('CaptureEventDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptureEventDirective();
    expect(directive).toBeTruthy();
  });
});
